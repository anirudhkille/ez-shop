export type TSignup = {
  name: string;
  email: string;
  password: string;
};

export type TLogin = {
  email: string;
  password: string;
};

export type TUser = {
  name: string;
  email: string;
  password: string;
};
